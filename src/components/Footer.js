import React, { PropTypes } from 'react'

function Footer({  }) {
  return (
    <footer className="text-center mb4 mt1 p1">
      <p className="bt1 pt2">
        More To Come.
      </p>
    </footer>
  )
}

Footer.propTypes = {
}
Footer.defaultProps = {
}

export default Footer
